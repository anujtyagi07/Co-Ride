{
  "name": "co-ride",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --prefix frontend && npm run build --prefix frontend",
    "vercel-install": "npm install --prefix backend"
  }
}
